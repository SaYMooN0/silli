program Main;
function Factorial(n : integer) : integer;
begin
  if n <= 1 then
    result := 1
  else
    result := n * Factorial(n - 1);
end;
var
  n,  answer : integer;
  text : string;
begin
  n := 5;
  answer := Factorial(n);
  text := convertIntegerToString(answer);
  printLine(text);
end.
